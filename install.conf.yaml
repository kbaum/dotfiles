- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.zshrc: zshrc
    ~/.bash_profile: bash_profile
    ~/.aliases: aliases
    ~/.functions: functions
    ~/.paths: paths
    ~/.vimrc: vim/vimrc
    ~/.vim: vim
    ~/.gitignore: gitignore
    ~/.gitconfig: gitconfig
    ~/.gemrc: gemrc
    ~/.git-completion.bash: git-completion.bash
    ~/bin: bin
    ~/.inputrc: inputrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)" || echo "already installed"
  - sh -c "npm install -g  prettier @prettier/plugin-ruby"
- brewfile:
    # This accepts the same options as `brew bundle` command:
    file: Brewfile
